# Core
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# RunPod serverless SDK (pin to 1.7.10 — last version before worker routing bug #432)
runpod==1.7.10

# SSL certificates for aiohttp (RunPod SDK uses aiohttp for webhook delivery)
certifi

# Config (needed to load FasterLivePortrait configs)
omegaconf>=2.3.0

# HuggingFace (model downloads)
huggingface-hub>=0.19.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0

# Image processing (opencv comes from FasterLivePortrait deps)
Pillow>=10.0.0
numpy>=1.24.0
