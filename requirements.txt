# RunPod serverless SDK
runpod>=1.6.0

# SSL certificates for aiohttp (RunPod SDK uses aiohttp for webhook delivery)
certifi

# Brotli for RunPod API response decoding
brotli

# HTTP requests (used in handler monkey-patches)
requests

# HuggingFace (model downloads)
huggingface-hub>=0.19.0
